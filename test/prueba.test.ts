import {describe, expect, test} from '@jest/globals'
import supertest from 'supertest'
import { server } from '../src/server'

describe("miPrimerTest", ()=>{

  // test("hola mundo", ()=>{
  //   expect(5).toEqual(5)
  // })

  // test("hola mundo", async()=>{
  //   const {text} = await supertest(server).get('/holamundo')

  //   expect(text).toEqual('hola mundo')
  // })


})